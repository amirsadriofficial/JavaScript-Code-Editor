export const SAMPLE_CODE = `// JavaScript Code Editor — try typing below
const greet = (name) => {
  console.log("Hello, " + name);
};

function sum(items) {
  return items.reduce((acc, n) => acc + n, 0);
}

const numbers = [1, 2, 3, 4, 5];
greet("world");
console.log(sum(numbers));

// Tab accepts ghost text · Esc dismisses · Alt+] / Alt+[ cycles
`
