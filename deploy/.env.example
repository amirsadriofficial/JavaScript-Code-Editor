# Copy to deploy/.env and adjust for this server.
#   cp .env.example .env

# Host port published on loopback (must match upstream in nginx.conf)
APP_PORT=4020

# Optional npm registry mirror (use if registry.npmjs.org is unreachable)
# NPM_REGISTRY=https://registry.npmmirror.com
